"""
Enseñar a encapsular lógica en bloques de código reutilizables, ententiendo los
conceptos de parámetros y valores de retorno

Requisitos:

- Define una función 'calcular_area_rectangulo(base, altura)' que reciba dos argumentos y 
retorne el área calculada
- Define una función 'calcular_area_circulo(radio)'. Dentro de ella, importa el módulo math para
usar math.pi y retorne el área
- En el bloque principal del script (fuera de las funciones), pide al usuario los datos necesarios
para un rectángulo, llama la función correspondiente, y muestra el resultado
- Repite el proceso para un círculo

"""